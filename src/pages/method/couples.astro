---
import BaseLayout from '../../layouts/BaseLayout.astro'
import site from '../../content/pages/site.json'

const title = `אימון זוגי בשיטת מ.ס.ע | ${site.brand}`
const description = 'כשאותו ויכוח חוזר שוב ושוב, זה כמעט תמיד דפוס. עובדים מהשורש, בלי דרמות ובלי טלאים.'
---

<BaseLayout title={title} description={description}>
  <main class="animate-fade-in py-24 bg-background min-h-screen">
    <div class="max-w-4xl mx-auto px-6">

      <header class="mb-10 text-center">
        <h1 class="text-5xl md:text-6xl font-black text-primary font-display mb-6">
          אימון זוגי בשיטת מ.ס.ע
        </h1>
        <p class="text-xl text-primary/70 font-body">
          {description}
        </p>
      </header>

      <div class="bg-white p-10 md:p-16 rounded-[3rem] shadow-elegant border border-white">
        <h2 class="text-2xl font-black text-primary font-display mb-6">
          במקום לתקן את הריב, מבינים את הדפוס
        </h2>

        <p class="text-primary/80 font-body leading-relaxed mb-8">
          כשאחד נסגר והשני נלחץ, כשיש ביקורת מול מגננה, כשמשהו קטן מדליק אש גדולה
          זה לא “אופי”. זו מערכת הגנה. ובדיוק שם עובדים.
        </p>

        <div class="grid md:grid-cols-3 gap-6">
          <div class="bg-background p-6 rounded-2xl border border-accent/10">
            <div class="font-black text-accent mb-2">מיפוי</div>
            <div class="text-primary/80 font-body">
              מזהים מה מפעיל כל אחד ומה קורה רגע לפני הפיצוץ.
            </div>
          </div>
          <div class="bg-background p-6 rounded-2xl border border-accent/10">
            <div class="font-black text-accent mb-2">סילוק</div>
            <div class="text-primary/80 font-body">
              מפרקים את הדפוס שמנהל אתכם ומחזירים בחירה.
            </div>
          </div>
          <div class="bg-background p-6 rounded-2xl border border-accent/10">
            <div class="font-black text-accent mb-2">עצמאות</div>
            <div class="text-primary/80 font-body">
              בונים שפה זוגית חדשה, לא רק “שלום בית” זמני.
            </div>
          </div>
        </div>

        <div class="mt-10">
          <a
            href={site.primaryCtaHref}
            class="inline-flex items-center gap-3 px-8 py-4 rounded-2xl gradient-gold text-white font-bold shadow-gold"
          >
            {site.primaryCtaLabel} <span aria-hidden="true">←</span>
          </a>
        </div>
      </div>

    </div>
  </main>
</BaseLayout>
