---
import site from '../content/pages/site.json';
const year = new Date().getFullYear();
---
<footer class="bg-primary text-white py-20 border-t border-teal-light relative overflow-hidden">
  <div class="max-w-7xl mx-auto px-6 flex flex-col md:flex-row justify-between gap-16 text-center md:text-right items-center md:items-start relative z-10">
    <div class="max-w-xs mx-auto md:mx-0">
      <div class="flex items-center justify-center md:justify-start gap-4 mb-8">
        <div class="w-12 h-12 gradient-gold rounded-full flex items-center justify-center shadow-lg">
          <img src="/assets/logo.png" alt="לוגו" class="w-7 h-7" />
        </div>
        <span class="font-black text-3xl font-display tracking-tight">{site.brand}</span>
      </div>
      <p class="text-gray-300 font-body text-base leading-relaxed mb-8">אימון רגשי תודעתי בנתניה ובזום.</p>
      <div class="text-sm text-gray-400 font-body">{site.fullName}</div>
    </div>

    <div class="text-sm text-gray-400 font-body self-end flex flex-col gap-2 md:items-start items-center">
      <span>© {year} {site.fullName} - {site.brand}.</span>
      <a href="/accessibility" class="hover:text-accent underline decoration-accent/30">הצהרת נגישות</a>
    </div>
  </div>
</footer>
